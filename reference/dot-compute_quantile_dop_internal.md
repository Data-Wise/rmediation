# Internal: Compute quantile using Meeker & Escobar (1994) algorithm

Internal: Compute quantile using Meeker & Escobar (1994) algorithm

## Usage

``` r
.compute_quantile_dop_internal(
  p,
  mu.x,
  mu.y,
  se.x,
  se.y,
  rho = 0,
  lower.tail = TRUE
)
```
