% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prod3_core.R
\name{pprodnormal3}
\alias{pprodnormal3}
\alias{p_prod3}
\title{Cumulative Distribution Function for the Product of Three Normal Variables}
\usage{
pprodnormal3(
  q,
  mean,
  cov,
  method = c("gauss", "hcubature"),
  tol = 1e-06,
  nodes = NULL,
  diagnostics = FALSE
)

p_prod3(...)
}
\arguments{
\item{q}{Numeric scalar quantile.}

\item{mean}{Numeric vector of means of length 3.}

\item{cov}{3x3 covariance matrix. Must be positive-definite, except that a
single zero-variance component is permitted (the problem then reduces to
the two-variable product-normal case). Perfect correlation between two
components (\verb{|rho| = 1}) or an indefinite matrix is rejected.}

\item{method}{Integration method. \code{"gauss"} (default) is tensor-product
Gauss-Legendre quadrature on a domain partitioned at the coordinate axes,
with the node count escalated until successive rules agree to \code{tol}.
\code{"hcubature"} is the adaptive method used before version 1.7.0; it is
retained for cross-checking and backward comparison, but is \strong{not
recommended} when the \verb{(X, Y)} block is ill-conditioned (see Note).}

\item{tol}{Numeric tolerance passed to the integration routine; must be
strictly positive.}

\item{nodes}{Optional integer: force a fixed number of Gauss-Legendre nodes
per dimension instead of escalating adaptively. Ignored when
\code{method = "hcubature"}. Mainly useful for reproducing a specific rule or
for cross-checking a result at two node counts.}

\item{diagnostics}{Logical. If \code{TRUE}, the returned value carries \code{"error"}
and \code{"nodes"} attributes. Defaults to \code{FALSE} so the return value stays a
bare numeric for existing callers.

What \code{"error"} means depends on \code{method}, and the two are \strong{not
comparable}:
\itemize{
\item \code{method = "gauss"}: the gap between the last two quadrature rules, a
genuine convergence estimate for discretisation error. It does not see
truncation error, which is instead bounded by construction.
\item \code{method = "hcubature"}: that integrator's \emph{own} reported error estimate.
Treat it with suspicion. It is satisfied at exactly the configurations
where the result is badly wrong (see Note), which is why it cannot be
used to detect the failure this method was replaced over.
}

\code{"error"} is \code{NA} when \code{nodes} is supplied: a single fixed rule produces no
successive-rule gap to measure. To check convergence at a fixed rule,
evaluate at \code{nodes} and \code{2 * nodes} and compare.}

\item{...}{Additional arguments (unused; present for the \code{p_prod3}
deprecated alias).}
}
\value{
Probability \code{P(X1 * X2 * X3 <= q)} as a numeric scalar in \verb{[0, 1]}.
}
\description{
Computes \code{P(X1 * X2 * X3 <= q)} where \verb{(X1, X2, X3)} follows a trivariate
normal distribution with mean vector \code{mean} and covariance matrix \code{cov}.
\code{Z} is marginalized analytically through the conditional Gaussian structure
\verb{Z | (X, Y)}, and the remaining two-dimensional integral is evaluated with
adaptive cubature (dimension reduction; see Tofighi, 2026).
}
\note{
For a fully degenerate point mass (all variances zero) with zero means,
\code{q == 0} returns \code{0.5} by the mid-distribution convention rather than \code{0}
or \code{1}.

Before version 1.7.0 the default integrator was \code{"hcubature"}, which
returns materially wrong values on ill-conditioned covariance matrices --
at \code{rho = 0.999} the error reaches 24\% at \code{q = 0.5} and 92\% in relative
terms in the lower tail, silently and with no diagnostic. The default is
now \code{"gauss"}, which agrees with \code{"hcubature"} to roughly \code{1e-6} on
well-conditioned input and remains accurate where \code{"hcubature"} fails.
Results from the two methods therefore differ slightly on benign input and
substantially on ill-conditioned input; the latter is the correction.
}
\section{Note}{

\code{p_prod3()} is a superseded alias for \code{pprodnormal3()}, kept for backward
compatibility; use \code{pprodnormal3()} in new code.
}

\examples{
Sigma <- diag(3)
pprodnormal3(q = 0, mean = c(0, 0, 0), cov = Sigma)
}
\seealso{
\code{\link[=pprodnormal]{pprodnormal()}} and \code{\link[=qprodnormal]{qprodnormal()}} for the two-variable product-
normal CDF/quantile; \link{ProductNormal3} for the corresponding S7 class.
}
