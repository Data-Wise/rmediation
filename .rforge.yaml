site:
  allowlist:
    # Tooling config files required at the package root / vignettes/ by their
    # respective tools; pkgdown has no config knob to exclude root *.md files
    # from build_home_md() (hardcoded handled-list), so these are accepted
    # as intentionally-published rather than relocated.
    - CLAUDE.md
    - GEMINI.md
    - vignettes/.gitignore
    - vignettes/_quarto.yml
